import React from 'react';
import spinnerGif from '../Res/icons/spinner.gif';

const Spinner = () => {
    return (
        <img src={spinnerGif} alt='spinner' className='spinner'/>
    )
}

export default Spinner;